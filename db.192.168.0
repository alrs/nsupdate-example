$TTL	40
0.168.192.in-addr.arpa.  IN	SOA	localhost. root.localhost. (
			      1		; Serial
			     60		; Refresh
			     60		; Retry
			    120		; Expire
			     20 )	; Negative Cache TTL
;
@	IN	NS	localhost.

1.0.168.192.in-addr.arpa.	IN PTR	router.bind.test.
